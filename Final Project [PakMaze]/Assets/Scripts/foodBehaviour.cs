using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class foodBehaviour : MonoBehaviour
{
    /*float currentlyMoved = 0;
    public float movementDistance = 1000;
    public bool movingUp = true;
    float move = 0.01f;
    float increaseInterval = 1;
    Vector3 objectPos;

    // Start is called before the first frame update
    void Start()
    {
        objectPos = transform.position;
        public float maxHeight = objectPos.y + 10;
        public float minHeight = objectPos.y - 10;
    }

    

    // Update is called once per frame
    void Update()
    {
        Vector3 pos = this.transform.position;
        if (pos.y < maxHeight)
        {
            pos.y += 1;
        }

        else 
        {
            pos.y -= 1;
        }
    }

    void moveRules()
    {
        move = 0.01f;
        if (currentlyMoved == movementDistance)
        {
            increaseInterval *= -1;
            movingUp = false;
        }
        else if (currentlyMoved == -1)
        {
            increaseInterval *= -1;
            movingUp = true;
        }

        if (movingUp == false)
        {
            move = -0.01f;
        }
        currentlyMoved += increaseInterval;
        this.transform.Translate(0, move, 0);
    }*/

}

